<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="kids_action" model="ir.actions.act_window">
        <field name="name">Kids</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">hospital.patient</field>
        <field name="view_mode">tree,kanban,form</field>

        <field name="context">{'default_note':'You will be Oky'}</field>
        <field name="domain">[('age', '&lt;=', 17)]</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new patient details!
            </p>
        </field>
    </record>
    <menuitem id="menu_kids_patient"
              name="Kids"
              parent="menu_patient_root"
              action="kids_action"
              sequence="10"/>

</odoo>